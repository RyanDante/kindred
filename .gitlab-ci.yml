stages:
  - build

variables:
  IMAGE: yourdockerhubusername/kindred:latest

build:
  stage: build
  image: docker:24
  services:
    - docker:dind

  script:
    - docker login -u $DOCKERHUB_USERNAME -p $DOCKERHUB_TOKEN
    - docker build -t $IMAGE .
    - docker push $IMAGE